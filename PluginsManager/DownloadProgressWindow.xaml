<Window x:Class="PluginManagerWPF.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="下载进度" Height="200" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusLabel" Grid.Row="0" Text="准备下载..." Margin="0,0,0,10"/>

        <ProgressBar x:Name="DownloadProgressBar" Grid.Row="1" Height="20" Margin="0,0,0,5"
                     Minimum="0" Maximum="100" Value="0"/>

        <Grid Grid.Row="2" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="速度:" FontWeight="Bold"/>
            <TextBlock x:Name="SpeedLabel" Grid.Column="1" Text="--" Margin="5,0,0,0"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="剩余时间:" FontWeight="Bold"/>
            <TextBlock x:Name="EtaLabel" Grid.Column="1" Text="--" Margin="5,0,0,0"/>
        </Grid>

        <Button x:Name="CancelButton" Grid.Row="4" Content="取消" Width="80" Height="25" 
                HorizontalAlignment="Right" Click="CancelButton_Click"/>
    </Grid>
</Window>